#version 330 core
out vec4 FragColor;

// adapted from https://learnopengl.com/Lighting/Basic-Lighting

void main()
{
    FragColor = vec4(1.0f, 0.75f, 0.0f, 1.0f); // set alle 4 vector values to 1.0
}